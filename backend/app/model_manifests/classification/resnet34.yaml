model_name: resnet34
label: ResNet34
task_type: classification
model_family: resnet
supports_compare: true
supports_search: true
is_default: false
display_order: 80
builder:
  type: torchvision_classifier
  torchvision_name: resnet34
  backbone_component_name: resnet34_native
  backbone_extractor: resnet_stages
  native_head_attr: fc
  feature_dim: 512
default_model_recipe:
  version: model_recipe@v1
  task_type: classification
  model_family: resnet
  base_model: resnet34
  input_channels: 3
  width_multiple: 1.0
  components:
    backbone:
      name: resnet34_native
      params: {}
    neck:
      name: avg_pool
      params: {}
    head:
      name: native_classifier
      params: {}
  backbone_config:
    stem_variant: standard
    attention_module: none
    last_channel_multiplier: 1.0
  head_config:
    pooling_type: avg
    classifier_dropout: 0.0
    classifier_type: linear
  modules: {}
  metadata:
    notes: Default ResNet34 recipe using the torchvision backbone and classifier.
parameter_space:
  model_name: resnet34
  version: resnet34@v1
  editable_params:
    optimizer:
      type: enum
      choices: [sgd, adam, adamw]
    learning_rate:
      type: number_range
      min: 0.0001
      max: 0.01
    batch_size:
      type: discrete_values
      choices: [32, 64, 128, 256]
    image_size:
      type: number_range
      min: 1
      max: 10000
    epochs:
      type: discrete_values
      choices: [10, 20, 30, 50]
    weight_decay:
      type: number_range
      min: 0.0
      max: 0.01
    scheduler:
      type: enum
      choices: [none, step, cosine]
    mixup_alpha:
      type: number_range
      min: 0.0
      max: 1.0
    cutmix_alpha:
      type: number_range
      min: 0.0
      max: 1.0
    random_erasing_prob:
      type: number_range
      min: 0.0
      max: 0.5
    loss_name:
      type: enum
      choices: [cross_entropy, cross_entropy_with_label_smoothing, focal_loss]
    focal_gamma:
      type: number_range
      min: 0.5
      max: 5.0
    label_smoothing:
      type: number_range
      min: 0.0
      max: 0.2
    neck_name:
      type: enum
      choices: [avg_pool, gem_pool]
    head_name:
      type: enum
      choices: [native_classifier, linear, dropout_linear]

